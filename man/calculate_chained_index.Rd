% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_chained_index.R
\name{calculate_chained_index}
\alias{calculate_chained_index}
\title{Calculate a Chained Index (Annual Overlap Method)}
\usage{
calculate_chained_index(
  dataset,
  method,
  period_variable,
  dependent_variable,
  numerical_variables = NULL,
  categorical_variables = NULL,
  reference_period = NULL,
  ...
)
}
\arguments{
\item{dataset}{Data frame with input data}

\item{method}{The index method to use for the short-term segments (e.g., "fisher", "timedummy", "hmts")}

\item{period_variable}{Name of the column containing time periods (e.g., "2020Q1", "2020M01")}

\item{dependent_variable}{Name of the dependent variable (usually price)}

\item{numerical_variables}{Vector of numeric quality-determining variables (default = NULL)}

\item{categorical_variables}{Vector of categorical variables (default = NULL)}

\item{reference_period}{Period to normalize the final index to (e.g., "2015")}

\item{...}{Additional arguments passed to the underlying calculate_hedonic_index method (e.g. window_length)}
}
\value{
A data.frame with the chained index series
}
\description{
Calculates a price index by chaining short-term indices. The method splits the data into years.
For the first year, a standard index is calculated. For subsequent years, an index is calculated
including the last period of the previous year (the overlap period). These short-term indices
are then linked (chained) together to form a continuous series.
}
\details{
This approach is often used to update index weights annually or to reduce the impact of
structural changes in the market over long periods.
}
\author{
Vivek Gajadhar
}
